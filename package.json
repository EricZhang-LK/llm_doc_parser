{
  "name": "llm-doc-parser-hooks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "commitlint": "commitlint --from=HEAD~1 --to=HEAD"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "husky": "^9.0.11"
  },
  "engines": {
    "node": ">=18"
  }
}
